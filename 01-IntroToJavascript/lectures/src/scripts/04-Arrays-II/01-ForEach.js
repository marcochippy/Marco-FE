// 1.
const numbers = [1, 2, 3, 4, 5];

// 2.
numbers.forEach((number) => {
  console.log(number);
});

// 3.
sum = 0;
numbers.forEach((number) => {
  sum = sum + number;
});
console.log(sum);

// 4.
